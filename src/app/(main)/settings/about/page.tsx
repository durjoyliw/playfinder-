import { SettingsPlaceholder } from "../settings-shared";

export default function Page() {
  return (
    <SettingsPlaceholder
      title="About PlayFinder"
      description="PlayFinder — find players, join teams, and get in the game."
    />
  );
}
